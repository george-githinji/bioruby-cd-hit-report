#class SeqData
  #def initialize(fasta_file)
    #Bio::FlatFile.auto(fasta_file).map{ |f| f}
    #lines = File.readlines(names_file).map {|line| line.chomp!}
    #puts lines.map {|line| entries.select {|entry| entry.definition == line }}
  #end
#end
